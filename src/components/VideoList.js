import React from 'react'
import VideoItem from './VideoItem'

import { Grid } from '@materal-ui/core'

const VideoList = ({ videos }) => {

    const listOfVideos = videos.map((video) =>{
        <Video
    } )
    return (
        <h1>Video List</h1>
    )
}

export default VideoList